export type DataType = {
  createdAt: Date;
  name: string;
  avatar: string;
  age: number;
  address: string;
  id: string;
};
export const arrayKey = ["id", "name", "age", "avatar", "address", "createdAt"];
